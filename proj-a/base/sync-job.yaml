apiVersion: batch/v1
kind: Job
metadata:
  name: print
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
      - name: print
        image: test-placeholder
        command: ["/bin/sh"]
        args: ["-c", "date"]
      restartPolicy: Never